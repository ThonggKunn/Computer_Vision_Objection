# ============================================================
# Computer Vision — AI Hỗ Trợ Người Khiếm Thị
# Requirements
# ============================================================
# Cài đặt:
#   pip install -r requirements.txt
# ============================================================

# ── Core AI / Vision ─────────────────────────────────────
ultralytics>=8.0.0          # YOLOv8 detection + segmentation
torch>=2.0.0                # PyTorch (MPS cho Apple M1)
torchvision>=0.15.0

# ── Xử lý ảnh ───────────────────────────────────────────
opencv-python>=4.8.0        # camera + vẽ frame
Pillow>=10.0.0              # PIL Image, vẽ text tiếng Việt
numpy>=1.24.0

# ── Text To Speech ───────────────────────────────────────
gTTS>=2.3.0                 # Google Text-to-Speech tiếng Việt

# ── Dataset Pipeline ─────────────────────────────────────
icrawler>=0.6.6             # crawl ảnh Google/Bing
imagehash>=4.3.1            # progress bar
kaggle>=1.5.16              # phát hiện ảnh trùng (perceptual hash)
tqdm>=4.65.0                # tải dataset từ Kaggle
albumentations>=1.3.0       # data augmentation nâng cao
matplotlib>=3.7.0           # vẽ biểu đồ phân phối dataset

# ── Tiện ích ─────────────────────────────────────────────
requests>=2.31.0            # HTTP requests